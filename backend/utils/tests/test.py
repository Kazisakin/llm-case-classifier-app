# Superseded by test_main.py, which uses FastAPI's TestClient against an
# isolated in-memory DB and a mocked Claude client instead of requiring a
# live server on localhost:8000. Kept only so this filename doesn't 404 for
# anyone with an old link to it; safe to delete.
